[project]
name = "rbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psycopg-pool>=3.3.0",
    "psycopg2-binary>=2.9.12",
    "psycopg[binary]>=3.3.3",
    "python-dotenv>=1.2.2",
]
